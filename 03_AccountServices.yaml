#spring.application.name=03_AccountServices
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.url=jdbc:mysql://localhost:3306/accounts_db
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
#logging.file.name=logs/accounts.log
#server.port=8181

spring:
  application:
    name: "03_AccountServices"
  datasource:
        username: root 
        password: 
        url: jdbc:mysql://localhost:3306/accounts_db
        driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active:
      - "prod"
logging:
  file:
    name: logs/accounts.log
server:
  port: 8181
#CUSTOM PROPERTIES - Used in application
build:
  version: "1.0"
account:
  message: "Welcome to MSEDCL bank account REST API - Default"
  contactDetails:
    name: "DEFAULT"
    email: "default@msedcl.in"
  onCallSupport:
    - 1234567890
    - 9876543210